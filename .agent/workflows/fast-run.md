---
description: Run the React Native app quickly on the Android emulator
---

This workflow automates the process of opening the Android emulator and running the app.

1. Ensure you are in the `mobile_app` directory.
// turbo
2. Run the fast execution script:
```powershell
powershell -ExecutionPolicy Bypass -File .\fast-run.ps1
```
